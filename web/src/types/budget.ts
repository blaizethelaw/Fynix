export type Income = { id: string; name: string; amount: number }
export type Expense = { id: string; category: string; amount: number }
export type DebtInput = { balance: number; apr: number; payment: number }
export type InvestInput = { principal: number; monthly: number; rate: number; years: number }
